# Agent Operating Guide

本文件只保存立即生效的硬规则。当前 `AGENTS.md` 所在目录即库根，不硬编码盘符、用户名或旧工作区。

## 启动与读取

1. 上下文已包含本协议时不要重复读取，直接进入 `重要记忆/00_AI阅读入口.md`。
2. 按核心对象选择一个主事实入口，再补齐会影响事实、风险、工具、用户偏好和验收的附加入口。
3. 七个双层目录默认先读 `任务路由.md`；根 README 和目录 README 只在需要地图、全量盘点或路由失效时读取。
4. 用户明确指定具体文件时可直接读取；仍须补齐安全、来源和验收入口。

## 写回

- 普通任务默认只读；普通总结、分析和复盘不自动等于入库。
- 用户明确要求写入、维护、整理，或任务本身在更新项目/日期资产时才写回。
- 普通任务唯一的自动写回例外，是具有真实来源、跨任务复用价值、明确预防规则且不重复的经验教训。
- “整理记忆库”按 `Inbox/README.md` 完成原件、处理件、正式写回、验收和最终移动的完整事务。
- 保留原件和冲突口径；摘要、状态与判断必须回链来源。
- 新文件采用统一 Schema；旧文件只在下次实质触碰时渐进迁移。

## 安全与隔离

- 不写入密码、token、API key、Cookie、私人联系方式、临时授权链接、浏览器 Profile 或登录态。
- 用户画像只保存已确认的长期事实和偏好；群 ID、执行身份、账号目标、权限和其它操作配置放在库外。
- 默认不进入 Agent/编辑器运行态、版本库内部、自动化缓存、虚拟环境、`node_modules`、`site-packages`、包缓存、第三方源码、项目 `archives/`、Inbox 已入库原件与临时区。
- 搜索、链接检查、统计和索引盘点必须在目录枚举前剪枝；不得先递归全库再过滤结果。
- `董事会/` 只有在用户明确说出“启动董事会决策”，或明确要求维护该目录时才读取、写入或校验；普通咨询、多角度分析、方案评审、复杂任务或仅提到董事会均不得触发，也不得主动建议启动。

## 项目与文件

- 工作产物进入 `project/<项目名>/`；根目录只放正式入口、一级目录和版本控制文件。
- 项目 README 顶部保留 `当前状态`、`最后更新`、`关键产物`、`下一步`、`事实来源`。
- 活跃项目按 README 显式“最后更新”排序；文件 mtime 和深层生成物不代表业务状态。
- 只有用户明确说明项目完结时才移入 `project/已完结项目/`；普通存档不改变项目状态。
- 项目快照放入项目自身 `archives/<时间>_<说明>/`，旧快照默认只读。
- 不自动删除 `Inbox/临时文件/`；“临时”命名和任务完成都不构成删除授权。

## 操作与跨环境

- 不编造姓名、日期、数据、认证、奖项、链接、项目状态或工具能力。
- Markdown 使用 UTF-8；命令和脚本优先使用相对路径、当前目录或脚本自身位置。
- 当前任务依赖本地工具且电脑、Agent、盘符、授权或能力状态不明时，先读跨电脑能力复用入口并做只读环境验证。
- 历史插件、版本、路径、登录态和授权不代表当前可用；在当前环境重新核验。
